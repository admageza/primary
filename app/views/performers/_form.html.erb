<%= form_with(model: performer, local: true) do |form| %>
  <% if performer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(performer.errors.count, "error") %> prohibited this performer from being saved:</h2>

      <ul>
      <% performer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :performer_title %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image, id: :performer_image %>
    <%= form.hidden_field :image_cache %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :performer_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
