<div class="tab-content text-center" style="margin-top:10%">
  <div class="row" style="margin-left:10%">
    <% @primaries.each do |primary| %>
    <div class="col-sm-3 card">
      <%= primary.title %>
      <%= link_to image_tag(primary.image.url, :class => "art card"), primary %>
    </div>
    <% end %>
     </div>
   <div class="col-sm-12 " style="margin-left:-40%">
      <%= paginate @primaries, :outer_window => 3 %>
   </div>
   <div class="col-sm-12 ">
      <%= link_to 'Back to home page', root_path, class: 'btn btn-prymary'  %>
   </div>
</div>