<%= form_with(model: maternaire, local: true) do |form| %>
  <% if maternaire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(maternaire.errors.count, "error") %> prohibited this maternaire from being saved:</h2>

      <ul>
      <% maternaire.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :maternaire_title %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image, id: :maternaire_image %>
     <%= form.hidden_field :image_cache %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :maternaire_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
